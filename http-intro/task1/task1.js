// Your task is to return info about the user with the given ID;
// Get info from users.json;
// Example: localhost:3000/users/1 -> should return 'sabadoryo'

import express from 'express';
const app = express()
const port = 3000

app.get('do something here', (req, res) => {
  'do something here'
})

app.listen(port, () => {
  console.log(`User info app is started at port: ${port}`)
})